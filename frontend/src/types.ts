
export type Userdata = {
    id: number
    admin: boolean
    name: string,
    email: string
    password: string
    rfid: string
}

export type MachineData = {
    name: string,
    origin_id: string
}